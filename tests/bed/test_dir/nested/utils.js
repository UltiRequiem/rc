// Nested file with comments
export const utils = {
    // utility functions
    log: (msg) => console.log(msg), // logging function
    /* 
     * Error handler
     */
    error: (err) => console.error(err)
};
